# Auto-generated by ma-provider-tools — do not edit manually.
[project]
name = "ma-provider-yandex-ynison"
description = "Yandex Music Connect (Ynison) provider for Music Assistant"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = [
  "ya-passport-auth>=1.3.0",
  # Provider imports `music_assistant_models` at runtime; pin the minimum
  # version that ships AudioSource / SourceControl / ResourceBusyError
  # (introduced for MA's AudioSource MediaItem contract).
  "music-assistant-models>=1.1.120",
]

[project.optional-dependencies]
test = [
  "pytest>=9.0",
  "pytest-asyncio>=0.24",
  "pytest-aiohttp>=1.1",
  "pytest-cov>=7.0",
  "syrupy>=5.0",
  "codespell>=2.4",
  "music-assistant @ git+https://github.com/music-assistant/server.git@dev",
  "music-assistant-models>=1.1.120",
  # Upstream MA's auth_providers imports hass-client unconditionally; not yet
  # in their pyproject. Listed here so test installs work without manual help.
  "hass-client>=1.2",
  "aiohttp>=3.9.0",
  "ruff>=0.14",
  "mypy>=1.19",
  "pre-commit>=4.5",
  "pre-commit-hooks>=6.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = {file = "VERSION"}

[tool.setuptools.packages.find]
where = ["."]
include = ["provider*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: integration tests requiring a real MA server instance",
]
filterwarnings = [
  "ignore:coroutine.*was never awaited:RuntimeWarning",
]

[tool.codespell]
# >>> ma-provider-tools sync (codespell skip) — DO NOT EDIT >>>
skip = "*.js,*.svg,docs-site/package-lock.json"
# <<< ma-provider-tools sync (codespell skip) <<<

# >>> ma-provider-tools sync (mypy) — DO NOT EDIT >>>
[tool.mypy]
python_version = "3.14"
platform = "linux"
follow_imports = "silent"
ignore_missing_imports = true
check_untyped_defs = true
disable_error_code = ["annotation-unchecked", "import-not-found", "import-untyped"]
disallow_any_generics = true
disallow_incomplete_defs = true
disallow_subclassing_any = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
disallow_untyped_defs = true
enable_error_code = ["ignore-without-code", "redundant-self", "truthy-iterable"]
extra_checks = false
local_partial_types = true
no_implicit_optional = true
no_implicit_reexport = true
packages = ["tests", "provider"]
show_error_codes = true
strict_equality = true
strict_optional = true
warn_incomplete_stub = true
warn_no_return = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_configs = true
warn_unused_ignores = true
# <<< ma-provider-tools sync (mypy) <<<

[[tool.mypy.overrides]]
module = "tests.*"
warn_unused_ignores = false
