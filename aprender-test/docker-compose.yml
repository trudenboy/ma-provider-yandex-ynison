# APrender functional test — no real audio output
# UI: http://localhost:7779

services:
  aprender:
    build: .
    ports:
      - "7779:7779"
    privileged: true
    volumes:
      - ./bin/aprenderer:/opt/aprender/aprenderer:rw
      - ./fake-asound:/fake-asound:ro
    restart: "no"
